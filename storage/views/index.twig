<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ pageTitle }}</title>
    <meta name="description" content="{{ metaDescription }}">

    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website">
    <meta property="og:url" content="{{ app.request.uri }}">
    <meta property="og:title" content="{{ pageTitle }}">
    <meta property="og:description" content="{{ metaDescription }}">
    <meta property="og:image" content="{{ ogImage }}">

    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image">
    <meta property="twitter:url" content="{{ app.request.uri }}">
    <meta property="twitter:title" content="{{ pageTitle }}">
    <meta property="twitter:description" content="{{ metaDescription }}">
    <meta property="twitter:image" content="{{ ogImage }}">
    {% if twitterUsername %}
        <meta name="twitter:creator" content="{{ twitterUsername }}">
    {% endif %}

    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link rel="canonical" href="{{ app.request.uri }}">
</head>
<body class="bg-gray-50 font-sans leading-normal tracking-normal">

<main class="max-w-7xl mx-auto py-6 sm:px-6 lg:px-8">
    <div class="px-4 py-6 sm:px-0">
        <h1 class="text-4xl font-extrabold text-gray-900 sm:text-5xl sm:tracking-tight lg:text-6xl text-center mb-4">{{ frameworkName }}</h1>
        <p class="text-xl text-gray-500 text-center max-w-3xl mx-auto mb-8">{{ description }}</p>
        <p class="text-indigo-600 font-semibold text-center mb-6">Version {{ version }}</p>

        <div class="my-6 text-center">
            <a href="https://github.com/yabasi/framework" target="_blank" rel="noopener noreferrer" class="inline-flex items-center px-6 py-3 border border-transparent text-base font-medium rounded-md shadow-sm text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500">
                Explore on GitHub
            </a>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
            {% for title, description in features %}
                <div class="bg-white overflow-hidden shadow rounded-lg">
                    <div class="px-4 py-5 sm:p-6">
                        <h3 class="text-lg leading-6 font-medium text-gray-900 mb-2">{{ title }}</h3>
                        <p class="text-sm text-gray-500">{{ description }}</p>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
</main>

<footer class="bg-white border-t border-gray-200 mt-12">
    <div class="max-w-7xl mx-auto py-6 px-4 sm:px-6 lg:px-8 text-center text-gray-500">
        <p>&copy; {{ year }} {{ frameworkName }}. All rights reserved.</p>
    </div>
</footer>
</body>
</html>